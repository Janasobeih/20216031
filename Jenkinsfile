pipeline {
    agent any
    stages {
        stage('List Files') {
            steps {
                // Correctly call the 'ls' command using 'sh'
                sh 'ls -la'
            }
        }
    }
}
